<h1><%= @song.name %></h1>
<h2>by <%= @song.artist.name %></h2>

<% @song.genres.each do |genre| %>
  <%= genre.name %><br>
  <% end %>

<br><br>
<!-- <%= link_to "Delete", '/songs/[][song.id]', method: :delete %> -->
<%= link_to "Delete", "/songs/#{@song.id}", method: :delete %>